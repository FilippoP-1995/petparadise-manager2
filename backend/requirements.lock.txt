# Lock riproducibile - Release hardening (gate 5).
#
# requirements.txt resta la fonte delle dipendenze DIRETTE con range
# (>=X,<Y) - invariato, e' l'intento del progetto, non toccato qui.
# Questo file e' invece l'elenco ESATTO (dirette + transitive) di cio' che
# gira davvero, generato con `pip freeze` da un venv pulito installato da
# requirements.txt e con l'intera suite pytest verde (395 test).
#
# Nessun nuovo strumento aggiunto (pip-tools/uv/poetry): sono scelte di
# tooling reali con tradeoff diversi, fuori dal perimetro di una
# correzione di hardening - `pip freeze`, gia' incluso in pip stesso,
# resta l'unica opzione che non richiede una decisione architetturale.
#
# Per rigenerare dopo un aggiornamento deliberato di requirements.txt:
#   python -m venv .venv-relock && .venv-relock/Scripts/activate
#   pip install -r requirements.txt
#   pip freeze > requirements.lock.txt
#   (poi eseguire la suite pytest contro questo venv prima di committare)
#
# Per un'installazione riproducibile (es. CI/deploy):
#   pip install -r requirements.lock.txt
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
Mako==1.4.1
MarkupSafe==3.0.3
packaging==26.3
pluggy==1.6.0
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
Pygments==2.21.0
pytest==8.4.2
pytest-asyncio==0.26.0
python-dotenv==1.2.3
PyYAML==6.0.3
SQLAlchemy==2.0.52
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
watchfiles==1.2.0
websockets==17.0.1
